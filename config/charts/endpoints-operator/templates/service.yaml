apiVersion: v1
kind: Service
metadata:
  name: {{ include "endpoints-operator.fullname" . }}
  labels:
    {{- include "endpoints-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "endpoints-operator.selectorLabels" . | nindent 4 }}
